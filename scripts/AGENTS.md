# Scripts

Utility scripts for development and CI/CD.

## Guidelines

- Scripts should be either Python (preferred) or Bash (`.sh`).
- Python scripts do not require tests, but should follow the best practices defined in lib/AGENTS.md.
- Keep scripts focused on a single task / purpose.
- Use descriptive filenames that indicate the script's purpose.
