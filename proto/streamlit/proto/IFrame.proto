syntax = "proto3";

message IFrame {
  oneof type {
    // A URL to load
    string src = 1;

    // Inline HTML
    string srcdoc = 2;
  }

  float width = 3;
  bool has_width = 4;
  float height = 5;

  bool scrolling = 7;
}
