# AI QA Testing workflow runs QA tests on a PR branch using Cursor CLI
name: AI QA Testing

on:
  pull_request:
    types: [labeled]
  workflow_dispatch:
    inputs:
      pr_number:
        description: "PR number to run QA tests on"
        required: true
        type: number
      model:
        description: "Model to use for QA testing"
        required: false
        type: string
        default: "claude-4.6-opus-high-thinking"

env:
  PR_NUMBER: ${{ github.event.inputs.pr_number || github.event.pull_request.number }}
  MODEL: ${{ github.event.inputs.model || 'claude-4.6-opus-high-thinking' }}

jobs:
  check-secrets:
    if: github.event_name == 'workflow_dispatch' || github.event.label.name == 'ai-qa-test'
    runs-on: ubuntu-latest
    outputs:
      has_key: ${{ steps.check-key.outputs.has_key }}
    steps:
      - name: Check for API key
        id: check-key
        env:
          CURSOR_API_KEY: ${{ secrets.CURSOR_API_KEY }}
        run: |
          if [ -z "$CURSOR_API_KEY" ]; then
            echo "has_key=false" >> $GITHUB_OUTPUT
            echo "::warning::CURSOR_API_KEY is not available. This typically happens for PRs from forks."
          else
            echo "has_key=true" >> $GITHUB_OUTPUT
          fi

  ai-qa-testing:
    needs: check-secrets
    if: needs.check-secrets.outputs.has_key == 'true'
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    permissions:
      contents: read
      pull-requests: read
      issues: read
      actions: read

    concurrency:
      group: ${{ github.workflow }}-pr-${{ github.event.inputs.pr_number || github.event.pull_request.number }}
      cancel-in-progress: true

    defaults:
      run:
        shell: bash

    steps:
      - name: Get PR details
        id: pr-info
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPOSITORY: ${{ github.repository }}
          PR_NUMBER_ENV: ${{ env.PR_NUMBER }}
        run: |
          PR_DATA=$(gh api "repos/$REPOSITORY/pulls/$PR_NUMBER_ENV")
          echo "head_sha=$(echo "$PR_DATA" | jq -r '.head.sha')" >> "$GITHUB_OUTPUT"
          echo "head_ref=$(echo "$PR_DATA" | jq -r '.head.ref')" >> "$GITHUB_OUTPUT"
          echo "base_ref=$(echo "$PR_DATA" | jq -r '.base.ref')" >> "$GITHUB_OUTPUT"
          echo "html_url=$(echo "$PR_DATA" | jq -r '.html_url')" >> "$GITHUB_OUTPUT"
          echo "title=$(echo "$PR_DATA" | jq -r '.title')" >> "$GITHUB_OUTPUT"

      - name: Checkout Streamlit code
        uses: actions/checkout@v6
        with:
          ref: ${{ steps.pr-info.outputs.head_sha }}
          persist-credentials: false
          submodules: "recursive"
          fetch-depth: 0

      - name: Set Python version vars
        uses: ./.github/actions/build_info

      - name: Setup virtual env
        uses: ./.github/actions/make_init
        with:
          python_version: ${{ env.PYTHON_MAX_VERSION }}

      - name: Install playwright
        uses: ./.github/actions/playwright_install

      - name: Build frontend
        run: make frontend-fast

      - name: Install Cursor CLI
        run: |
          curl https://cursor.com/install -fsS | bash
          echo "$HOME/.cursor/bin" >> $GITHUB_PATH

      - name: AI QA Testing
        env:
          CURSOR_API_KEY: ${{ secrets.CURSOR_API_KEY }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPOSITORY: ${{ github.repository }}
          PR_NUMBER_ENV: ${{ env.PR_NUMBER }}
          PR_HTML_URL: ${{ steps.pr-info.outputs.html_url }}
          PR_BASE_REF: ${{ steps.pr-info.outputs.base_ref }}
          PR_HEAD_REF: ${{ steps.pr-info.outputs.head_ref }}
          PR_TITLE: ${{ steps.pr-info.outputs.title }}
        run: |
          # Create work-tmp directory first
          mkdir -p work-tmp

          # Write prompt to file using heredoc to avoid shell injection from user-controlled values
          # Use awk to safely substitute environment variables
          cat > work-tmp/prompt-template.txt << 'PROMPT_EOF'
          You are operating in a GitHub Actions runner to perform QA testing on a pull request.

          ## Context

          - Repository: %%REPOSITORY%%
          - Main branch: develop
          - PR Number: %%PR_NUMBER_ENV%%
          - PR URL: %%PR_HTML_URL%%
          - PR Title: %%PR_TITLE%%
          - Base Ref: %%PR_BASE_REF%%
          - Head Ref: %%PR_HEAD_REF%%

          The repository is checked out at the PR's head commit with full git history.
          The dev environment is fully set up - you can run make commands.
          Playwright browsers are installed for testing.
          The frontend is built and ready.

          The GitHub CLI (gh) is available for READ operations to get PR details, diff, and file contents.

          ## Task

          Perform comprehensive QA testing on the feature implemented in this PR:

          1. **Understand the changes**: Read the PR diff, any linked specs or issues, and relevant documentation to understand what this PR implements.

          2. **Create a QA test plan**: Based on the changes, create a test plan covering:
             - Happy path scenarios
             - Edge cases and error handling
             - Cross-browser compatibility (if UI changes)
             - Accessibility considerations
             - Performance implications

          3. **Execute CI-safe testing**: Use only non-interactive, headless, time-bounded commands that exit on their own. Do not use `make debug`, `make frontend-dev`, or any other long-running dev server. Prefer targeted repo-supported commands such as `make check`, `make python-tests`, `make frontend-tests`, and `make run-e2e-test <filename>` for relevant Playwright/E2E coverage. If you run shell commands directly, ensure each one has an explicit timeout so the workflow cannot hang.

          4. **Document findings**: Write a comprehensive QA report including:
             - Test scenarios executed
             - Issues found (with reproduction steps)
             - Screenshots or logs for any failures
             - Overall assessment (pass/fail/needs-work)

          ## Output

          Write your QA report to `work-tmp/qa-report.md` (create the directory if needed).

          The report should use this structure:

          ```markdown
          # QA Testing Report

          ## PR Summary
          Brief description of what was tested.

          ## Test Plan
          List of test scenarios.

          ## Test Results
          | Scenario | Status | Notes |
          |----------|--------|-------|
          | ...      | ...    | ...   |

          ## Issues Found
          Detailed description of any issues, with reproduction steps.

          ## Screenshots/Evidence
          Any relevant screenshots or logs.

          ## Verdict
          **PASS** / **FAIL** / **NEEDS_WORK**

          Brief justification for the verdict.
          ```
          PROMPT_EOF

          # Safely substitute environment variables using awk
          awk '{
            gsub(/%%REPOSITORY%%/, ENVIRON["REPOSITORY"]);
            gsub(/%%PR_NUMBER_ENV%%/, ENVIRON["PR_NUMBER_ENV"]);
            gsub(/%%PR_HTML_URL%%/, ENVIRON["PR_HTML_URL"]);
            gsub(/%%PR_TITLE%%/, ENVIRON["PR_TITLE"]);
            gsub(/%%PR_BASE_REF%%/, ENVIRON["PR_BASE_REF"]);
            gsub(/%%PR_HEAD_REF%%/, ENVIRON["PR_HEAD_REF"]);
            print
          }' work-tmp/prompt-template.txt > work-tmp/prompt.txt

          PROMPT=$(cat work-tmp/prompt.txt)
          cursor-agent -p "$PROMPT" --force --model "$MODEL" --output-format=text || true

          # Ensure report exists (fallback if cursor-agent fails)
          mkdir -p work-tmp
          if [ ! -f work-tmp/qa-report.md ]; then
            echo "# QA Testing Report" > work-tmp/qa-report.md
            echo "" >> work-tmp/qa-report.md
            echo "QA testing did not produce a report." >> work-tmp/qa-report.md
            echo "" >> work-tmp/qa-report.md
            echo "## Verdict" >> work-tmp/qa-report.md
            echo "**UNKNOWN**" >> work-tmp/qa-report.md
          fi

          # Extract verdict for labeling from the Verdict section only
          # Use sed to extract the Verdict section, then grep with -F for fixed-string matching
          # Check FAIL and NEEDS_WORK first to avoid false positives from test result tables
          VERDICT_SECTION=$(sed -n '/^## Verdict/,/^## /p' work-tmp/qa-report.md | head -5)
          if echo "$VERDICT_SECTION" | grep -qF "**FAIL**"; then
            echo "FAIL" > work-tmp/verdict.txt
          elif echo "$VERDICT_SECTION" | grep -qF "**NEEDS_WORK**"; then
            echo "NEEDS_WORK" > work-tmp/verdict.txt
          elif echo "$VERDICT_SECTION" | grep -qF "**PASS**"; then
            echo "PASS" > work-tmp/verdict.txt
          else
            echo "UNKNOWN" > work-tmp/verdict.txt
          fi

      - name: Upload QA artifacts
        uses: actions/upload-artifact@v7
        with:
          name: qa-testing-output
          path: |
            work-tmp/qa-report.md
            work-tmp/verdict.txt
          retention-days: 7

  post-results:
    needs: [check-secrets, ai-qa-testing]
    if: always() && needs.check-secrets.result != 'skipped'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Remove 'ai-qa-test' label
        if: github.event_name == 'pull_request'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER_ENV: ${{ env.PR_NUMBER }}
          REPOSITORY: ${{ github.repository }}
        run: |
          gh pr edit "$PR_NUMBER_ENV" --remove-label "ai-qa-test" --repo "$REPOSITORY" || true

      - name: Download QA artifacts
        id: download
        if: needs.ai-qa-testing.result == 'success'
        uses: actions/download-artifact@v8
        continue-on-error: true
        with:
          name: qa-testing-output
          path: qa-artifacts

      - name: Post missing key notice
        if: needs.check-secrets.outputs.has_key == 'false'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER_ENV: ${{ env.PR_NUMBER }}
          REPOSITORY: ${{ github.repository }}
        run: |
          gh pr comment "$PR_NUMBER_ENV" --body "ℹ️ **AI QA Testing Not Available via Label**

          The \`ai-qa-test\` label cannot trigger AI QA testing for this PR because the required API key is not available. This typically happens for PRs from forked repositories.

          **To request AI QA testing, a maintainer can:**
          1. Go to the [AI QA Testing workflow](https://github.com/streamlit/streamlit/actions/workflows/ai-qa-testing.yml)
          2. Click **Run workflow**
          3. Enter the PR number: \`$PR_NUMBER_ENV\`
          4. Click **Run workflow**" --repo "$REPOSITORY"

      - name: Post QA report
        if: needs.check-secrets.outputs.has_key == 'true' && steps.download.outcome == 'success'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER_ENV: ${{ env.PR_NUMBER }}
          REPOSITORY: ${{ github.repository }}
          MODEL_NAME: ${{ env.MODEL }}
        run: |
          if [ -f qa-artifacts/work-tmp/qa-report.md ]; then
            # Add footer to report
            {
              echo ""
              echo "---"
              echo "*This is an automated QA report by \`$MODEL_NAME\`.*"
            } >> qa-artifacts/work-tmp/qa-report.md

            gh pr comment "$PR_NUMBER_ENV" --body-file qa-artifacts/work-tmp/qa-report.md --repo "$REPOSITORY"
          else
            gh pr comment "$PR_NUMBER_ENV" --body "⚠️ **AI QA Testing completed but no report was generated.**

            Please check the [workflow run](https://github.com/$REPOSITORY/actions/runs/${{ github.run_id }}) for details." --repo "$REPOSITORY"
          fi

      - name: Post failure notice
        if: needs.check-secrets.outputs.has_key == 'true' && (needs.ai-qa-testing.result == 'failure' || steps.download.outcome != 'success')
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER_ENV: ${{ env.PR_NUMBER }}
          REPOSITORY: ${{ github.repository }}
          SERVER_URL: ${{ github.server_url }}
          RUN_ID: ${{ github.run_id }}
        run: |
          gh pr comment "$PR_NUMBER_ENV" --body "⚠️ **AI QA Testing Failed**

          The AI QA testing job failed to complete. Please check the [workflow run](${SERVER_URL}/$REPOSITORY/actions/runs/${RUN_ID}) for details.

          You can retry by adding the 'ai-qa-test' label again or manually triggering the workflow." --repo "$REPOSITORY"

      - name: Apply label based on verdict
        if: needs.check-secrets.outputs.has_key == 'true' && steps.download.outcome == 'success'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER_ENV: ${{ env.PR_NUMBER }}
          REPOSITORY: ${{ github.repository }}
        run: |
          if [ ! -f qa-artifacts/work-tmp/verdict.txt ]; then
            echo "Warning: verdict.txt not found, skipping label"
            exit 0
          fi

          VERDICT=$(cat qa-artifacts/work-tmp/verdict.txt | tr -d '[:space:]')
          case "$VERDICT" in
            FAIL)
              echo "Adding 'do-not-merge' label"
              gh pr edit "$PR_NUMBER_ENV" --add-label "do-not-merge" --repo "$REPOSITORY"
              ;;
            PASS)
              echo "Removing 'do-not-merge' label if present"
              gh pr edit "$PR_NUMBER_ENV" --remove-label "do-not-merge" --repo "$REPOSITORY" || true
              ;;
            *)
              echo "Verdict is $VERDICT - no label changes"
              ;;
          esac

      - name: Write step summary
        env:
          QA_RESULT: ${{ needs.ai-qa-testing.result }}
          HAS_KEY: ${{ needs.check-secrets.outputs.has_key }}
        run: |
          {
            echo "## AI QA Testing Summary"
            echo "- PR: #$PR_NUMBER"
            echo "- Model: $MODEL"
            echo "- Result: $QA_RESULT"
            if [ "$HAS_KEY" = "false" ]; then
              echo "- Note: API key not available (fork PR)"
            fi
            if [ -f qa-artifacts/work-tmp/verdict.txt ]; then
              echo "- Verdict: $(cat qa-artifacts/work-tmp/verdict.txt)"
            fi
          } >> "$GITHUB_STEP_SUMMARY"
