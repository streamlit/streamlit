---
name: Run Semgrep Checks

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read

jobs:
  run-semgrep-reusable-workflow:
    uses: snowflakedb/reusable-workflows/.github/workflows/semgrep-v2.yml@85711dfade75b7463e57d4ed0908c2505d313eec # main
    secrets:
      token: ${{ secrets.SEMGREP_APP_TOKEN }}
