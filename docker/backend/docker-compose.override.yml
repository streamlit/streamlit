version: '3.6'

services:
  streamlit:
    volumes:
      - ../../lib:/streamlit
      - ../credentials.toml:/root/.streamlit/credentials.toml
      - ../config.toml:/root/.streamlit/config.toml
      - ../../examples:/examples
    command: /examples/images.py
