syntax = "proto3";

import "Delta.proto";
import "ObjectId.proto";

// A message across a stream.
message StreamlitMsg {
  oneof type {
    ObjectId new_notebook = 1;
    DeltaList delta_list = 2;
  }
}
