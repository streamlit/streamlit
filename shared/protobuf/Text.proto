syntax = "proto3";

// Formatted text
message Text {

  // Content to display.
  string body = 1;

  // Content format
  enum Format {
    PLAIN = 0;
    MARKDOWN = 1;
    JSON = 2;
  }
  Format format = 2;
}
